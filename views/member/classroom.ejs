<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>القاعة الدراسية - العلم نور</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700&display=swap" rel="stylesheet">
    <!-- إضافة FontAwesome لعرض الأيقونات -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/member/style.css">

    <style>
       .modal {
      display: none;
      position: fixed;
      z-index: 1;
      left: 0;
      top: 0;
      width: 100%;
      height: 100%;
      background-color: rgba(0, 0, 0, 0.5);
    }

    /* تنسيق محتوى النافذة */
    .modal-content {
      background-color: #fff;
      margin: 10% auto;
      padding: 20px;
      border-radius: 8px;
      width: 80%;
      max-width: 500px;
      text-align: center;
      position: relative;
      box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.3);
    }

    /* زر إغلاق النافذة */
    .close {
      position: absolute;
      top: 10px;
      right: 15px;
      color: #aaa;
      font-size: 28px;
      font-weight: bold;
      cursor: pointer;
    }
    .close:hover {
      color: #000;
    }

    /* تنسيق الجدول */
    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 10px;
    }

    table, th, td {
      border: 1px solid #ddd;
    }

    th, td {
      padding: 10px;
      text-align: center;
    }

    th {
      background-color: #007BFF;
      color: white;
    }

    /* زر لفتح النافذة */
    #openModal {
      padding: 10px 20px;
      background-color: #007BFF;
      color: #fff;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      margin-top: 20px;
    }
    </style>
</head>
<body class="classroom-page">
    <header>
        <nav>
            <div class="logo">العلم نور</div>
            <div class="nav-links">
                <a href="/member/dashboard">القاعات الدراسية</a>
            </div>
        </nav>
    </header>

    <main class="classroom-layout">
        <aside class="lessons-sidebar">
            <h2 id="classroom-title">عنوان القاعة</h2>
            <div class="lessons-list" id="lessons-list">
                <!-- سيتم إضافة الدروس هنا بواسطة JavaScript -->
            </div>
        </aside>

        <section class="lesson-content">
            <div id="lesson-container">
                <h3 id="lesson-title">اختر درساً من القائمة</h3>
                <div class="video-container" id="video-container">
                    <!-- سيتم إضافة الفيديو هنا بواسطة JavaScript -->
                </div>
                <!-- زر دخول عبر زوم -->
                <div class="zoom-button-container">
                    <button id="zoom-join-button" value="" class="zoom-join-button" style="display: none;">
                        <i class="fas fa-video"></i> دخول الاجتماع عبر زوم
                    </button>
                    
                    <button id="calendar-button" value="" class="zoom-join-button"  >
                        <i class="fa-solid fa-calendar-days"></i> عرض مواعيد القاعة
                    </button>
                </div>

                <div>
                    <div class="quiz-button-container">
                        <button id="quiz-button" class="quiz-button" style="display: none;">
                            اختبار الدرس
                        </button>
                    </div>
                </div>
                
            </div>

            <div id="myModal" class="modal">
                <div class="modal-content">
                  <span class="close">&times;</span>
                  <h2>الجدول التقويمي للحصص</h2>
                  <table>
                    <thead>
                      <tr>
                        
                        <th>#</th>
                        <th>اليوم</th>
                        <th>الوقت</th>
                      </tr>
                    </thead>
                    <tbody id="scheduleTable">
                      <!-- سيتم تعبئة الجدول ديناميكيًا -->
                    </tbody>
                  </table>
                </div>
              </div>
            

        </section>
    </main>
    <script>
        const lessons = <%- JSON.stringify(lessons) %>;
        // يمكن إضافة كود JavaScript لتوليد الدروس وتشغيل الفيديوهات
    </script>

    <footer>
        <p>جميع الحقوق محفوظة &copy; 2024 - العلم نور</p>
    </footer>

    <!-- ملف جافاسكريبت الخاص بالصفحة -->
    <script src="/js/member/classroom.js"></script>
    <script>
        // حدث النقر على زر دخول عبر زوم
        document.getElementById("zoom-join-button").addEventListener("click", function(e){

            const link = e.target.value;
            // استبدل الرابط برابط اجتماع زوم الخاص بك
            window.open(link, "_blank");
        });
    </script>
</body>
</html>
